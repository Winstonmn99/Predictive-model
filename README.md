echo "# Predictive-model" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M master
git remote add origin https://github.com/Winstonmn99/Predictive-model.git
git push -u origin master
